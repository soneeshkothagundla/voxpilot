# Normalize line endings. Shell scripts MUST stay LF or the Docker build's
# `bash entrypoint.sh` fails with \r errors on a Windows checkout.
* text=auto
*.sh text eol=lf
*.ps1 text eol=crlf
*.bat text eol=crlf
*.png binary
*.ico binary
*.wav binary
